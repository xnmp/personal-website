/**
 * The watercolor-parchment backgrounds are retired — the theme now comes from
 * the terminal rice (see rice.css). Kept as a no-op so page slots remain for
 * future per-entry art.
 */
export function PageBackground(_props: { src: string }) {
  return null;
}
